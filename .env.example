# Environment variables for production deployment
# Copy this file to .env and fill in your actual values before deploying

# Flask Configuration
FLASK_ENV=production
DEBUG=False
PORT=5000

# API Keys
GEMINI_API_KEY=AIzaSyCi32c--eVwNHP5c0Cvq71G71FQdtFyNL0
GEMINI_MODEL=gemini-2.5-flash

# Database Configuration
# For Cloud SQL Server or Azure SQL Database
DB_SERVER=your-sql-server-address.database.windows.net
DB_NAME=ChatBoxDB
DB_USER=admin@yourserver
DB_PASSWORD=your-secure-password
DB_DRIVER={ODBC Driver 17 for SQL Server}
DB_TRUSTED_CONNECTION=False

# JWT Secret - Generate a strong random key
JWT_SECRET_KEY=generate-a-strong-random-key-here-at-least-32-characters

# Security
SECRET_KEY=another-strong-random-key-for-flask

# Admin Config
ADMIN_EMAIL=admin@yourdomain.com
ADMIN_PASSWORD=change-this-strong-password

# JWT Token Expiration (in seconds)
JWT_ACCESS_TOKEN_EXPIRES=86400

# CORS Configuration
CORS_ORIGINS=https://yourdomain.com,https://www.yourdomain.com

# Vector DB Path (cloud storage bucket path if using cloud)
VECTOR_DB_PATH=/app/data/vector_db
DOCUMENTS_PATH=/app/datasets
